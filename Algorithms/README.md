This implementation takes an number which has max 4 digits and it must be a positive number.
This algorithm divide the digits of this number and store them into a vector. All the operations
made by maths.